/*review badge*/
.mh-rev {
    margin: 5px 0 0;
}

// hide if no review caption
.spr-badge[data-rating="0.0"] {
  .spr-badge-caption {
      // hide for now
      // display: none;
  }
}

// alert
.alert {
    position: relative;
    margin: 25px 0 0;
    padding: 10px 15px;
    border-radius: 5px;
    color: #721c24;
    background-color: #f8d7da;
    border: 1px solid #f5c6cb;
}

.section-header {
    font-size: 30px;
    text-align: center;
    margin: 30px 0;
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* slideshow */
.template-index:not(.is-scroll){
  div#tidio-chat {
      // display: none;
      pointer-events: none;
  }
}
.theme-mh .no-js.ProductForm__Option {
    display: block !important;
}
